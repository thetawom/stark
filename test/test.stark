function main () as int {
    define arr as string[3];
    define i as int;
    arr <- {"Hello", "World", "!"};
    for i from 0 to 2 every 1 {
        prints(arr[i]);
    }
    arr[1] <- "Universe";
    for i from 0 to 2 every 1 {
        prints(arr[i]);
    }
}