function main () as int {
    define arr as int[3];
    define i as int;
    i <- 0;
    for i from 0 to 2 every 1 {
        arr[i] <- 2 * i + 1;
    }
    for each i in arr {
        print(i);
    }
    print(4 / 1);
    printf(4.0 / 0.0);
}